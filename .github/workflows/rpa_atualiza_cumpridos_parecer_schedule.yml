name: RPA Atualização de Cumpridos com Parecer - Agendamento Automático

on:
  schedule:
    # Executa às 13:00 e 16:15 (horário de São Paulo)
    # Cron: minuto hora dia mês dia_da_semana
    # 0 16 * * 1-5 = 16:00 UTC (13:00 BRT) - Segunda a Sexta
    # 15 19 * * 1-5 = 19:15 UTC (16:15 BRT) - Segunda a Sexta
    - cron: '0 16 * * 1-5'   # 13:00 BRT (16:00 UTC)
    - cron: '15 19 * * 1-5'  # 16:15 BRT (19:15 UTC)
  
  # Permite execução manual
  workflow_dispatch:
    inputs:
      test_mode:
        description: 'Modo de teste (sem interface gráfica)'
        required: false
        default: 'false'
        type: boolean

env:
  # Configuração do fuso horário
  TZ: America/Sao_Paulo
  
  # Configurações do RPA
  HEADLESS: true
  CI: true
  GITHUB_ACTIONS: true

jobs:
  rpa-atualiza-cumpridos-parecer:
    name: Executar RPA Atualização de Cumpridos com Parecer
    runs-on: ubuntu-latest
    
    # Configurar timezone
    defaults:
      run:
        shell: bash
    
    steps:
    - name: Configurar Timezone
      run: |
        sudo timedatectl set-timezone America/Sao_Paulo
        echo "Timezone configurado: $(date)"
    
    - name: Checkout do código
      uses: actions/checkout@v4
      
    - name: Configurar Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.12'
        cache: 'pip'
    
    - name: Instalar dependências do sistema
      run: |
        sudo apt-get update
        sudo apt-get install -y \
          libnss3 \
          libatk-bridge2.0-0 \
          libdrm2 \
          libxkbcommon0 \
          libxcomposite1 \
          libxdamage1 \
          libxrandr2 \
          libgbm1 \
          libxss1 \
          libasound2t64 \
          libatspi2.0-0 \
          libgtk-3-0 \
          libgdk-pixbuf2.0-0
    
    - name: Instalar dependências Python
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install playwright
        playwright install chromium
        # Instalar dependências do Playwright manualmente (evitar libasound2)
        sudo apt-get install -y \
          libnss3 \
          libatk-bridge2.0-0 \
          libdrm2 \
          libxkbcommon0 \
          libxcomposite1 \
          libxdamage1 \
          libxrandr2 \
          libgbm1 \
          libxss1 \
          libasound2t64 \
          libatspi2.0-0 \
          libgtk-3-0 \
          libgdk-pixbuf2.0-0
    
    - name: Configurar variáveis de ambiente
      run: |
        echo "NOVAJUS_USERNAME=${{ secrets.NOVAJUS_USERNAME }}" >> $GITHUB_ENV
        echo "NOVAJUS_PASSWORD=${{ secrets.NOVAJUS_PASSWORD }}" >> $GITHUB_ENV
        echo "SUPABASE_HOST=${{ secrets.SUPABASE_HOST }}" >> $GITHUB_ENV
        echo "SUPABASE_PORT=${{ secrets.SUPABASE_PORT }}" >> $GITHUB_ENV
        echo "SUPABASE_DATABASE=${{ secrets.SUPABASE_DATABASE }}" >> $GITHUB_ENV
        echo "SUPABASE_USER=${{ secrets.SUPABASE_USER }}" >> $GITHUB_ENV
        echo "SUPABASE_PASSWORD=${{ secrets.SUPABASE_PASSWORD }}" >> $GITHUB_ENV
    
    - name: Criar diretório de downloads
      run: |
        mkdir -p downloads
        echo "Diretório de downloads criado"
    
    - name: Executar RPA Atualização de Cumpridos com Parecer
      run: |
        echo "Iniciando RPA Atualização de Cumpridos com Parecer..."
        echo "Horário atual: $(date)"
        echo "Modo headless: $HEADLESS"
        echo "URL do relatório: https://robertomatos.novajus.com.br/agenda/GenericReport/?id=674"
        echo "Arquivo esperado: z-rpa_atualiza_cumpridos_com_parecer_rmb_queeue.xlsx"
        echo "Processamento: UPDATE na tabela agenda_base usando id_legalone como chave"
        
        # Executar o RPA de atualização
        python rpa_atualiza_cumpridos_com_parecer_rmb.py
        
        echo "RPA Atualização de Cumpridos com Parecer concluído!"
    
    - name: Verificar arquivos baixados
      run: |
        echo "Verificando arquivos na pasta downloads:"
        ls -la downloads/ || echo "Pasta downloads vazia ou não encontrada"
        
        # Verificar se há arquivos Excel
        find downloads/ -name "*.xlsx" -type f | head -5 || echo "Nenhum arquivo Excel encontrado"
    
    - name: Upload de logs (se houver erro)
      if: failure()
      uses: actions/upload-artifact@v4
      with:
        name: rpa-atualiza-cumpridos-parecer-logs-${{ github.run_number }}
        path: |
          downloads/
          debug_*.png
        retention-days: 7
    
    - name: Notificar sucesso
      if: success()
      run: |
        echo "✅ RPA Atualização de Cumpridos com Parecer executado com sucesso!"
        echo "📅 Data/Hora: $(date)"
        echo "🔄 Próxima execução: Verificar cron schedule"
        echo "📊 Processamento: UPDATE na tabela agenda_base"
    
    - name: Notificar falha
      if: failure()
      run: |
        echo "❌ RPA Atualização de Cumpridos com Parecer falhou!"
        echo "📅 Data/Hora: $(date)"
        echo "🔍 Verifique os logs para mais detalhes"

  # Job adicional para relatórios semanais
  weekly-report:
    name: Relatório Semanal - Atualização de Cumpridos com Parecer
    runs-on: ubuntu-latest
    if: github.event.schedule == '15 19 * * 5'  # Apenas às 16:15 de sexta-feira
    
    steps:
    - name: Gerar relatório semanal
      run: |
        echo "📊 Relatório Semanal - RPA Atualização de Cumpridos com Parecer"
        echo "📅 Data: $(date)"
        echo "✅ Execuções automáticas funcionando normalmente"
        echo "🔄 Próxima execução: Segunda-feira às 13:00 BRT"
        echo "📈 Horários de execução:"
        echo "   - 13:00 BRT (16:00 UTC)"
        echo "   - 16:15 BRT (19:15 UTC)"
